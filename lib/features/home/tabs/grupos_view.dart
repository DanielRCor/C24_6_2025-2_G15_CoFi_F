// Tab Grupos
import 'package:flutter/material.dart';

class GruposView extends StatelessWidget {
  const GruposView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla de Grupos'),
    );
  }
}